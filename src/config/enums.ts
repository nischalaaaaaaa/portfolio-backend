export enum CODES {
    DEFAULT = 1000,
    TOKEN_EXPIRED = 1001,
    REFRESH_TOKEN_EXPIRED = 1002,
    PROJECT_NULL = 1003,
}

export enum RESPONSE_TYPE {
    Error,
    Information,
    Question,
    Warning,
    Success,
}